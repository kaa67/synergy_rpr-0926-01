import SectionHeader from "components/SectionHeader";

const FirstBlood = () => {
  return (
    <section>
      <SectionHeader>Первый проект</SectionHeader>
    </section>
  );
};

export default FirstBlood;
